server {
    listen          80;
    server_name     ${HOSTNAME};
    location / {
        proxy_pass http://django:8000;
    }
    location /static {
        root /usr/share/nginx/whatmanager2/;
    }
}
